<% if place.tally != nil %>
  <span class='score'>
      <%= puts "This is #{place.id}" %>
      <span class='score-value' data-score="<%= place.id %>"><i class="fa fa-poll-h"></i><%= place.tally.percent %></span>
  </span>
<% end %>
