<% if place.tally != nil %>
  <span class='score'>
      <%= puts "This is #{place.id}" %>
     <span class='score-value' data-score="<%= place.id %>"><%= place.tally.percent %> %</span>
  </span>
<% end %>
